import React, { Component } from 'react';

//import { getCurrentUser } from '../actions/authentication';
//console.log(getCurrentUser());


export default class Home extends Component {
    render() {
        return (
            <div>
                Need to write homepage
            </div>
        );
    }
}

